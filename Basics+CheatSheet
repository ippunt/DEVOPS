https://dockerlabs.collabnix.com/docker/cheatsheet/
https://www.docker.com/wp-content/uploads/2022/03/docker-cheat-sheet.pdf

docker images; shows you images you have built, or docker pull; 
docker ps; List running containers
docker run httpd; Runs the container
docker ps -a; list running and stopped
docker run -p 80:80 httpd;  run an image with certain port
docker run -P httpd; map all ports
docker run -d httpd; start container in the background
docker run --hostname <hostname> httpd; assign hostname
docker run -name webtest -p 80:80 httpd;   Give it a name to easily manage; docker stop webtest; docker kill webtest;
docker run -it --entrypoint bash httpd;
docker exec -it container_id /bin/bash;  Get into bash of currently running container
